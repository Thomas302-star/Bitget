document.addEventListener('DOMContentLoaded',()=>{const items=document.querySelectorAll('.section:not(.hero),.service-card,.why-card,.trade-card,.start-card,.feature-card,.learn-more-card,.learn-more-panel,.trading-note,.overview-card,.notice,.footer-main,.footer-disclosure,.footer-bottom');items.forEach((item,index)=>{item.classList.add('reveal-on-scroll');const delay=index%5;if(delay)item.classList.add(`reveal-delay-${delay}`)});if(!('IntersectionObserver' in window)){items.forEach(item=>item.classList.add('is-visible'));return}const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}})},{threshold:.12,rootMargin:'0px 0px -40px 0px'});items.forEach(item=>observer.observe(item))});